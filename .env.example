POSTGRES_DB=videoai
POSTGRES_USER=videoai
POSTGRES_PASSWORD=videoai
DATABASE_URL=postgres://videoai:videoai@postgres:5432/videoai

TRAEFIK_PORT=8080

# Internal HTTP ports used inside the compose network.
WEBAPP_PORT=5173
API_PORT=8080
RENDER_PORT=8081

DEVASSETS_DIR=/data/devassets
RENDERS_DIR=/data/renders
THUMBNAILS_DIR=/data/thumbnails
