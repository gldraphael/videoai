const catalogPath = process.argv[2] ?? "devassets/catalog.yaml";

console.log("VideoAI devasset seed placeholder");
console.log(`Catalog: ${catalogPath}`);
console.log(
  "Real asset import, transcription, thumbnail generation, and database upserts are implemented by a later OpenSpec change."
);
